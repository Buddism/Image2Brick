struct Rectangle
{
	vec2<int> pos;
	vec2<int> scale;

	bool isOverlapping(Rectangle rect2)
	{
		bool x = (abs(pos.x - rect2.pos.x) * 2) <= (scale.x + rect2.scale.x);
		bool y = (abs(pos.y - rect2.pos.y) * 2) <= (scale.y + rect2.scale.y);
		return x && y;
	}
};#pragma once
